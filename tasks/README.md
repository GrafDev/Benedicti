# Tasks

This folder stores the BeneDicti AI development task board.

Use these states:

- `todo`: ready tasks.
- `review`: completed or blocked task waiting for Lead AI review.
- `done`: accepted task.

Each task should follow the format in `PROCESS.md`.

Builder reports are written inside the task file before moving it to `review`.
